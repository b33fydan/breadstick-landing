# Breadstick Landing

Standalone Vite + React landing page for Breadstick.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Netlify uses `npm run build` and publishes `dist`.
